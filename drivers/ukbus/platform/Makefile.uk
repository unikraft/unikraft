$(eval $(call addlib_s,libukbus_platform,$(CONFIG_LIBUKBUS_PLATFORM)))

CINCLUDES-$(CONFIG_LIBUKBUS_PLATFORM)   += -I$(LIBUKBUS_PLATFORM_BASE)/include
CXXINCLUDES-$(CONFIG_LIBUKBUS_PLATFORM) += -I$(LIBUKBUS_PLATFORM_BASE)/include

CINCLUDES-$(CONFIG_LIBUKBUS_PLATFORM) += -I$(LIBUKBUS_PLATFORM_BASE)/include

# TODO Remove as soon as plat dependencies go away
LIBUKBUS_PLATFORM_CINCLUDES-y += -I$(UK_PLAT_COMMON_BASE)/include

LIBUKBUS_PLATFORM_SRCS-y += $(LIBUKBUS_PLATFORM_BASE)/platform_bus.c
