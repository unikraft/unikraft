$(eval $(call addlib_s,libukefi,$(CONFIG_LIBUKEFI)))

ASINCLUDES-$(CONFIG_LIBUKEFI) += -I$(LIBUKEFI_BASE)/include
CINCLUDES-$(CONFIG_LIBUKEFI) += -I$(LIBUKEFI_BASE)/include
CXXINCLUDES-$(CONFIG_LIBUKEFI) += -I$(LIBUKEFI_BASE)/include
