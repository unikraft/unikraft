$(eval $(call addlib_s,libuksgx,$(CONFIG_LIBUKSGX)))

CINCLUDES-$(CONFIG_LIBUKSGX)    += -I$(LIBUKSGX_BASE)/include
CXXINCLUDES-$(CONFIG_LIBUKSGX)  += -I$(LIBUKSGX_BASE)/include

LIBUKSGX_SRCS-$(CONFIG_LIBUKSGX) += $(LIBUKSGX_BASE)/sgx_main.c
