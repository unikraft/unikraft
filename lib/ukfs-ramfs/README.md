# `ukfs-ramfs`: memory resident volatile filesystem

This library provides a RAM-resident filesystem, registered as the "ramfs" filesystem type under `ukfs`.
It provides no bespoke API of its own; for usage consult the documentation of `ukfs` and above layers.
