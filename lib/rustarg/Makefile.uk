$(eval $(call addlib_s,librustarg,$(CONFIG_LIBRUSTARG)))

CINCLUDES-$(CONFIG_LIBRUSTARG)	+= -I$(LIBRUSTARG_BASE)/include
CXXINCLUDES-$(CONFIG_LIBRUSTARG)	+= -I$(LIBRUSTARG_BASE)/include

LIBRUSTARG_SRCS-$(CONFIG_LIBRUSTARG_CHARSET_ASCII) += $(LIBRUSTARG_BASE)/argparse.rs
LIBRUSTARG_SRCS-$(CONFIG_LIBRUSTARG_CHARSET_UTF8) += $(LIBRUSTARG_BASE)/Cargo.toml
LIBRUSTARG_SRCS-y += $(LIBRUSTARG_BASE)/glue.c
