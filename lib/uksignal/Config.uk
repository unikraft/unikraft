menuconfig LIBUKSIGNAL
	bool "uksignal: Unikraft signals"
	default n
	select LIBNOLIBC if !HAVE_LIBC
	select LIBUKDEBUG
	select LIBUKALLOC
	select LIBUKSCHED
	select LIBPOSIX_PROCESS
	select LIBSYSCALL_SHIM
	select LIBSYSCALL_SHIM_LIBCSTUBS

if LIBUKSIGNAL
	config LIBUKSIGNAL_LIBCFNS_ENABLE
		bool "signal: Provide libc signal implementation"
		default n
endif
