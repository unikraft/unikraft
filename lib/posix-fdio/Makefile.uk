$(eval $(call addlib_s,libposix_fdio,$(CONFIG_LIBPOSIX_FDIO)))

CINCLUDES-$(CONFIG_LIBPOSIX_FDIO) += -I$(LIBPOSIX_FDIO_BASE)/include
CXXINCLUDES-$(CONFIG_LIBPOSIX_FDIO) += -I$(LIBPOSIX_FDIO_BASE)/include

LIBPOSIX_FDIO_SRCS-y += $(LIBPOSIX_FDIO_BASE)/fdio.c
LIBPOSIX_FDIO_SRCS-y += $(LIBPOSIX_FDIO_BASE)/fdstat.c
LIBPOSIX_FDIO_SRCS-y += $(LIBPOSIX_FDIO_BASE)/fdctl.c
