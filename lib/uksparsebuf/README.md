# uksparsebuf: Generic sparse buffers

Sparse buffers implement generic data structures that provide a logical memory buffer of indeterminate size, composed of both allocated and sparse areas.

For more information, consult the source documentation in `include/uk/sparsebuf.h`.
