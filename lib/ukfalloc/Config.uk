menuconfig LIBUKFALLOC
	bool "ukfalloc: Abstraction for physical memory allocators"
	default n
	select LIBUKDEBUG

if LIBUKFALLOC

config LIBUKFALLOC_STATS
	bool
	select LIBUKLIBID
	select LIBUKLOCK
	select LIBUKSTORE
	help
		Invisible option selected by frame allocator libraries

endif
