menuconfig LIBUKFALLOC
	bool "ukfalloc: Abstraction for physical memory allocators"
	default n
	select LIBUKDEBUG

if LIBUKFALLOC

config LIBUKFALLOC_STATS
	bool "Collect frame allocator statistics"
	default n

endif
