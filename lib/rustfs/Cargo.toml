[package]
name = "rust"
version = "0.1.0"
edition = "2018"
authors = ["Dennis Kobert <dennis@kobert.dev>"]

[lib]
crate-type = ["staticlib"]

[dependencies]
unikraft = {path="../ukrust"}

[build-dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
