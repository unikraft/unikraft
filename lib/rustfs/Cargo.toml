[package]
name = "rust"
version = "0.1.0"
edition = "2018"
authors = ["TrueDoctor"]

[lib]
crate-type = ["staticlib"]

[dependencies]
wee_alloc = {version="0.4", features = ["static_array_backend"]}

[build-dependencies]


[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
