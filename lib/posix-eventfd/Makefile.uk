$(eval $(call addlib_s,libposix_eventfd,$(CONFIG_LIBPOSIX_EVENTFD)))

CINCLUDES-$(CONFIG_LIBPOSIX_EVENTFD) += -I$(LIBPOSIX_EVENTFD_BASE)/include
CXXINCLUDES-$(CONFIG_LIBPOSIX_EVENTFD) += -I$(LIBPOSIX_EVENTFD_BASE)/include

LIBPOSIX_EVENTFD_SRCS-y += $(LIBPOSIX_EVENTFD_BASE)/eventfd.c

ifeq ($(CONFIG_LIBPOSIX_FDTAB), y)
UK_PROVIDED_SYSCALLS-$(CONFIG_LIBPOSIX_EVENTFD) += eventfd-1
UK_PROVIDED_SYSCALLS-$(CONFIG_LIBPOSIX_EVENTFD) += eventfd2-2
endif
