$(eval $(call addlib_s,libposix_pipe,$(CONFIG_LIBPOSIX_PIPE)))

CINCLUDES-$(CONFIG_LIBPOSIX_PIPE) += -I$(LIBPOSIX_PIPE_BASE)/include
CXXINCLUDES-$(CONFIG_LIBPOSIX_PIPE) += -I$(LIBPOSIX_PIPE_BASE)/include

LIBPOSIX_PIPE_SRCS-y += $(LIBPOSIX_PIPE_BASE)/pipe.c

UK_PROVIDED_SYSCALLS-$(CONFIG_LIBPOSIX_PIPE) += pipe-1
UK_PROVIDED_SYSCALLS-$(CONFIG_LIBPOSIX_PIPE) += pipe2-2
