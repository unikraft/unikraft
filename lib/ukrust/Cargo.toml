[package]
name = "unikraft"
version = "0.1.0"
edition = "2018"

[features]
log-crate = ["log"]

[dependencies]
unikraft-sys = { path = "../ukrust-sys" }
log = { version = "0.4", optional = true, features=["max_level_trace"]}
