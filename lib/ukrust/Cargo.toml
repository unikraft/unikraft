[package]
license-file = "../../COPYING.md"
name = "ukrust"
version = "0.1.0"
edition = "2021"

[lib]
crate_type = ["lib"]

[profile.dev]
panic = "abort"
lto = "no"

[profile.release]
panic = "abort"
lto = "no"
